<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <stroke
        android:color="@color/background_color_primary"
        android:width="2dp"
        android:dashWidth="10dp"
        android:dashGap="6dp" />
    <solid android:color="@color/nav_item_secondery"/>
    <corners android:radius="16dp"/>
</shape>
